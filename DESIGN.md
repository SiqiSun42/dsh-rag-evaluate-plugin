# dsh-rag-evaluate 设计文档

运行在 DSH 上的 RAG 系统评测插件。输入是一个用户工作区，输出是一份可追溯、可复现的评测报告与改进建议。

> 本文档是活文档。各步骤的实现细节、参数与接口在编码过程中逐步补充，当前只记录已确认的设计目标、约束与注意点。

---

## 1. 定位

插件自动发现工作区中的向量库与查询入口，结合用户确认构建评测集，对被测 RAG 系统执行检索与判定，产出指标、效度校验结论和失败归因。

**目标用户**：已有一套可运行的 RAG 系统（至少包含一个向量库和查询代码），希望了解其检索效果、定位瓶颈、判断下一步优化方向的开发者。

**非目标**：从零构建 RAG 系统。该职责属于配套的构建类插件。

---

## 2. 范围与边界

**一句话概括：本插件对被测系统只做一件事——新增一个保存评测内容的文件夹。** 不改动 RAG 或项目中其他现成代码，也不动向量库。要修改都是评测结束之后的事，不属于本插件。

### 2.1 职责范围

**插件负责**

- 自动发现向量库与查询入口，整理为候选链路
- 探测向量库的规模、维度、内容主题与质量缺陷
- 与用户确认查询形态、覆盖方向与评测规模
- 生成评测集并冻结为带版本的规格
- 执行检索、判定相关性、计算指标
- 校验单次运行结果的效度，失败时给出回退目标
- 分析结果并输出可追溯的结论

**插件不负责**


| 不做的事项                   | 理由                                                                                                                                       |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| 改写查询                     | 会改变被测系统的行为，得到的是生产环境中不存在的系统；若被测系统上游已有改写环节，还会形成双重改写。需要时另做配套插件，作为可选适配器接入 |
| 修改被测项目的任何文件       | 插件对被测工作区只读；新建的评测产物集中写入独立的输出目录（见 §7.1）                                                                                                                       |
| 修改或重建向量库             | 属于构建类插件的职责                                                                                                                       |
| 端到端生成式评测（首个版本） | 需要被测系统暴露生成环节，先完成检索层评测                                                                                                 |
| 在插件内部与用户交互         | 确认环节由 Agent 承担，插件只负责产出候选、校验与落盘                                                                                      |
| 把上游改写或下游后处理算进来 | 只测指定的查询入口本身，上下游环节当作不存在——否则测的就不是同一套东西。报告可以引用项目里已有的环节作为建议，但不实现、不测量 |
| 检测某个内容是否在向量库中 | 判断「库里缺什么」需要知道库里**应该**有什么，那是外部信息，插件无从得知；库的内容该由别的工具查。评测中若撞上这类问题可以顺带提一句，但这不是本插件的检测项 |

### 2.2 一个关键区分

**生成查询与改写查询是两件不同的事。**

- **生成查询**：生成和编辑考题本身。改变的是试卷，不是考生。**属于本插件的核心职责。**
- **改写查询**：变换送入被测系统的输入。**本插件明确不做。**

---

## 3. 核心设计决策


| 决策项       | 选择                                   | 备选方案与否决理由                                                     |
| ------------ | -------------------------------------- | ---------------------------------------------------------------------- |
| 插件形态     | 工具集，由 Agent 编排                  | 全自动流水线：理解查询逻辑无法确定性自动化，而指标计算不应交由模型完成 |
| 接入方式     | 通用适配器契约                         | 直接调用特定查询函数：更换被测系统即需重写                             |
| 适配器加载   | 长驻子进程，按行分隔的 JSON 通信       | 直接 import：被测系统的依赖会污染插件环境，且模型会被反复加载          |
| 发现粒度     | 链路（入口 × 参数 × 向量库）         | 文件粒度：一个向量库目录可能含多个集合，一个查询函数可能存在多种模式   |
| 发现失败处理 | 降级为询问，不直接终止                 | 硬终止：自动发现必然存在遗漏，会把本可评测的系统也排除在外             |
| 模型一致性   | 默认信任查询代码，异常时触发校验       | 前置校验：成本与流程复杂度高；但仍保留"未经校验"的标注与触发条件       |
| 查询形态确认 | 用户指认样例，冻结为规格               | 由用户描述：用户对自身系统的描述往往不准确                             |
| 检索与判定   | 分离，中间落盘                         | 边跑边判：判定标准必然需要调整，耦合会导致全量重跑                     |
| 判定执行     | 一次判定，推导全部指标                 | 每个指标单独判定：成本成倍增加，且结果之间可能互相矛盾                 |
| 指标口径     | 只报告可计算的指标，不可计算的显式标注 | 以近似值补齐：产生虚假的完整性                                         |
| 产物管理     | 分层落盘并带版本号                     | 仅存于内存：无法复现、无法回退、无法审计                               |
| 遇到异常时   | 只记录并提示用户，不在当前步骤展开调查 | 就地排查：消耗大量资源，且缺乏后续步骤才有的证据。原因分析属于第⑨步   |
| 链路取舍     | 第①步枚举全部，第②步开始前由用户选定   | 第①步就取舍：此时没有成本依据；一路全测：执行成本从第②步起就已产生     |
| 接入层形态   | 声明式优先，代码为例外且须过机械校验   | 直接让 Agent 写适配器：提示词约束不住"顺手优化"，生成代码处正是能力泄漏点 |
| 接入层保真度 | 镜像而非净化：系统原有机制不增不删     | 只约定"不添加"：容易忽略"不删除"，导致把系统特性当缺陷修掉             |
| 比例获取方式 | 用户只勾选主项，比例按规则自动推导     | 要求用户填百分比或标主次：用户答不准，且交互次数膨胀                   |
| 确认与冻结分离 | 第③步松散确认，用户明确点头后第④步才规范化 | 合并处理：改的是草稿还是规格分不清，版本号失去意义                     |
| 事实覆盖 | 最新实测覆盖早先浅层判断；记录规则/默认值的字段，用户改动后必须改写成实际生效的内容 | 一律追加不改：前后矛盾；一律不许改：错了也不能纠正。边界是必须有实测依据，且不得编造 |
| 回退 | 用户随时可回到任一步重做，受影响的下游全部重走；同一运行内原地更新同名产物 | 不允许回退：用户改主意就得重开一次运行，前面全白做；允许并存两版：报告依据哪一版说不清 |
| MCP 服务 | 保留但暂不注册工具，作为备用 | 拆掉：launcher 与 requirements 同 patch 相互引用，拆除易断裂；且后续可能需要常驻状态 |

---

## 4. 总体流程

```
① 清点 ─→ ② 探测画像 ─→ ③ 确认查询计划 ─→ ④ 冻结规格
                                              │
                                              ▼
⑨ 归因分析 ←─ ⑧ 效度校验 ←─ ⑦ 执行与判定 ←─ ⑥ 全量生成 ←─ ⑤ 打样
     │                            │
     └───────── 任一环节可回退 ────┘
```

每一步都产出落盘产物并带版本号，因此回退的代价是"丢弃该产物、重跑下游"，而非全部重来。

---

## 5. 适配器接入

插件不对被测系统的形态作任何假设。接入方式是由用户提供一个适配器，由**被测系统自身的解释器**启动为长驻子进程。

### 5.1 适配器需要提供的能力

- 描述自身：可用的向量库与集合、期望的查询形态、上游流水线构成、能返回哪些字段
- 执行检索：给定查询与返回条数，返回结构化的结果列表
- （可选）执行生成：返回端到端答案，供后续的生成式评测使用

具体接口形式在实现阶段确定。

### 5.2 采用子进程的原因

1. **依赖隔离**：被测系统所需的深度学习与向量库依赖不进入插件环境，插件环境保持轻量。
2. **模型常驻**：一次评测只启动一次，嵌入模型与重排模型只加载一次。
3. **故障隔离**：被测代码在被导入时可能即产生副作用，置于插件主进程内会连带影响插件本身。

### 5.3 能力约束的显式声明

适配器未提供的能力，必须在**第②步**（探测画像）报告给用户，而不是等到计算指标时才暴露。

典型例子：若适配器不返回文档标识，则召回率类指标无法计算。此时应明确告知用户缺少该项能力及其影响，而不是以近似值替代。

**术语约定**：表示"是否返回分数"这类能力时，指的是能否以可编程方式取得；仅以文本形式嵌在返回值中的不算返回。该项取值需能表达"存在但需解析"的状态，不得用 `false` 掩盖。

---

## 6. 各步骤

### ① 清点

**目标**：在被测工作区中清点向量库与查询入口是否存在，整理为候选链路清单。本步只做存在性清点，不执行被测代码。

本步的价值在于尽早暴露结构性问题——部件缺失、入口引用了不存在的集合、系统形态与预期不符。这类问题漏过之后，后续每一步都会建立在错误的前提上。

**注意点**

- 采用**链路粒度**而非文件粒度。一个向量库目录可能包含多个集合，构成逻辑上的多个库；一个查询函数可能存在多种参数组合，构成完全不同的检索链路；入口与库之间通常存在绑定关系。
- 发现失败不等于不存在。自动扫描必然有遗漏，应降级为向用户询问，由用户补充或指定，最后才考虑要求用户编写适配器。只有当用户明确确认不存在查询入口时，才终止流程并建议先行构建。
- 需要识别并报告与签名默认值不一致的实际调用参数。同一个函数的不同调用方式可能对应不同的检索链路，应分别列出。
- 链路需区分来源：在工作区调用点中找到的，与仅通过公开签名可达的，证据强度不同。
- 被测工作区不一定是会话工作区。被评测系统位于会话工作区的子目录时，取该子目录。
- 本步同时按 §7.1 处理输出目录：确定本次运行是新建还是接续已有运行。

### ② 探测画像

**目标**：在不依赖构建脚本的前提下，独立得出向量库的客观事实与内容概况，并对选中的查询链路做一次烟测与返回值形态判定。

**注意点**

- **严格按当前系统测评。** 本步开始执行，因此在开始前向用户确认一次：不改写查询、不调整参数、不改阈值、不补后处理。用户若希望先改系统，则停下等改造完成。
- **接入层是镜像，不是净化。** 被测系统原有的机制一律保留——前缀、返回条数、兜底、去重、重排、截断，有就有，没有就没有。**不添加，也不删除。** 系统返回的陈旧结果、可疑兜底、错误前缀都不属于要修的问题；那正是要被测出来的东西。
- **接入层优先声明式。** 能用"怎么调用、怎么切分、分数在哪、什么表示无结果"四项声明描述时，不写代码——没有 Agent 写的代码，就没有顺手优化的位置。声明描述不了时才允许写代码，且必须实现 `query_sent` / `raw` / `items` 三件套并通过两条机械校验：发出的查询逐字等于给定查询；按声明规则反解析条目后逐字等于原始返回值。
- **接入层冻结。** 接入层计算指纹写入产物，指纹变化则下游产物失效。这主要防"跑到第⑦步发现分数难看、回头去修接入层"。
- **兼容补丁与接入层分开。** 接入层总是需要；兼容补丁只在被测入口**根本无法运行**时，经用户同意后做最小改动，且**能通过烟测就不打补丁**。返回结果不好看不构成理由。
- **运行环境由用户提供。** 只在被测工作区内寻找候选，不扫描机器其他位置，也不默认复用工作区之外的环境。找不到或依赖不全时停下并报告，给出"由插件代为安装"与"用户自行安装"两个选项——**未经明确同意不得安装任何东西**。
- **本步分三段：轻量清点 → 用户选择 → 深度画像。** 轻量清点只做三件事（打开向量库、列出每个集合的名称与条目数、每集合瞄 2–3 条原文），做完立即交付用户确认，不继续。用户据此决定评测哪些链路。**深度画像只针对选中链路涉及的集合**，未涉及的不付出深度成本。未选中的链路仍完整保留在画像中，仅标记为未选中。
- **本步对入口的接触以一次烟测为终点。** 烟测判据分三档：调用抛异常 → 停下并询问是否打兼容补丁；能调用但返回全空或完全乱码 → 询问是否加载了正确的向量库，说明可以继续但意义不大，由用户决定；能调用且返回内容有意义 → 直接继续。返回内容"有意义"指可读文本、与查询同语种、且不是空串或纯符号。
- **不得依赖构建脚本。** 索引是历史产物，构建脚本反映的是当下状态，两者可能不一致。构建脚本对索引的实际构成不具备证明力。
- 无法直接获取的事实（如嵌入模型名称——向量库通常不存储该信息）只能推断。推断结果必须标注来源，不得与实测结果并列呈现。
- 模型一致性默认以查询代码为假设来源，仅在出现异常信号时触发验证。触发信号为维度不匹配、抽样内容与查询语言或主题明显不符。（分数分布异常集中不属于本步——那需要评测集，属于第⑦步。）
- **抽样前必须先做近似去重。** 若切分时使用了重叠窗口，相邻内容块会大量重复，直接随机抽样会反复命中同一段内容，得出错误的内容画像。
- 除属性之外还需输出质量体检结论：是否存在可用于溯源的元数据、多个集合之间的标识空间是否重叠、重复内容的占比、长度分布、是否混入目录或页眉页脚等噪声内容。这些结论直接决定后续指标是否可计算、是否可解释。
- `gaps` **只收"测量做不了"的缺失，不收"系统不好"的发现**。判据是没有它第⑦步能否跑出指标；阈值是否有效、召回高低、结果是否相关，都是第⑦步要测出来的结论。
- 内容主题的归纳结果需要用户确认，且应向用户展示少量抽样原文作为判断依据。

### ③ 确认查询计划

**目标**：确定评测集要模拟的**方向**（问什么）与**形态**（怎么问），各带比例。本步不生成任何题目或问法样例。

**注意点**

- 两个轴**相互正交**：形态管"怎么说"，方向管"问什么"，各自独立加权。形态是全局的，不按方向分别定——否则比例确认会变成一张没人愿意填的二维表。
- **方向从第②步落盘的样本池推导**，而不是要求用户凭空列举。先剔除目录页、页眉页脚等低质量内容；样本池不足以归纳时如实说明，并请用户补充他知道的线上方向。
- **方向和形态的数量不设上限。** 这是用户系统的事实和他的判断，不是本插件该限制的东西——20 个、50 个、100 个方向都可以，**不要因为数量多就劝用户缩减或自行合并**。插件只有两句话要说：**一、把统计后果算给他看**（约 25 条 ±20 点、45 条 ±15 点、95 条 ±10 点；要单独下结论的层数 × 每层所需样本量 ≤ 计划总量），他接受只出总分就没问题；**二、限制本插件自己给用户看多少**——见第⑤步，参考样例固定上限 20 条。「方向 × 形态」的上限没有行业标准可套（这个交叉结构是本插件自己的设计）；NIST 的组合测试结论**对本设计不适用**——只有两个因素时成对覆盖就等于全交叉，反而支持覆盖所有格子。任何数量上的限制都只约束「给用户看什么」和「哪些层级能单独下结论」，不约束生成。
- **比例不让用户计算。** 用户只做"选"：勾选最常见的方向，再回答一句"是否还有可能出现其他方向"。比例按规则自动推导——主项均分 90%，其他均分 10%，没有其他时全部均分 100%。两个轴各自套用。
- **推导结果必须可复算**：同时记录用户的原始选择与推导规则，而不只是推导出的权重。
- 汇总呈现**尽量简洁**，让用户一眼看出哪里不对并直接改。比例允许反复调整，这属于自由交互，不计入确认次数。
- **验收标准要主动问，并写进规格。** 分两类：数值线（"HitRate@3 要到 0.9"）与偏好型（"多返回几条无所谓，但该有的不能漏"）。**偏好型更有用**——它直接决定建议的方向（宁漏不错 → 降阈值；宁错不漏 → 升阈值）。**不要替用户编一条验收线**；没有就是没有，报告如实说明无法判断达标。
- 汇总时提示**可报告性**：尾部方向在最终规模下可能只能合并报告，不会有独立数字。
- **形态清单必须以真实语料的例子呈现，不能只给类别名。** 把"自然语言问句""口语化简问"这类标签并排放着，用户看不出区别；正确做法是拿同一段语料内容，用各种形态各写一条，例子在前、类别在后。每项一个说明性例子即可；**成批的、用于校准生成倾向的样例属于后续步骤**。
- 用户在自由交互中给出的**真实查询**要原样记录，其可信度高于生成样本。

### ④ 冻结规格

**目标**：将第③步的确认结果固化为可复用、可版本化的规格文件，作为后续生成与复现的依据。

**注意点**

- 规格是评测集可复现性的锚点。规格变更应当提升版本号，而非原地覆盖。不同规格产出的报告不可直接比较，报告中必须注明所依据的规格版本。
- **规模不应以向量库大小的百分比确定。** 所需的样本量取决于需要在哪个层级上得出结论：报告层级由方向、形态、难度等维度交叉构成，各交叉单元的样本量决定了该单元能否单独下结论。向量库规模只构成上限——不应生成超过去重后有效内容块数量的问题，否则同一内容块被反复出题，等价于重复样本，会高估结果的置信度。
- 规格中必须声明哪些分层单独报告、哪些合并报告。总量有限时，部分交叉单元的置信区间会宽到无法支撑结论。
- 规格必须包含判定标准。判定标准未明确时，同一评测集会得出不同结果。
- 规格还必须包含：语料快照信息、各分层的样本数量、需要排除的出题范围、生成过程的相关设定。
- 规格应当产出两种形式：供程序读取的结构化形式，以及由前者渲染、供用户确认与事后查阅的可读形式。两者同源，不手工维护两份。

### ⑤ 打样

**目标**：按已冻结的规格做几件样品，交用户确认。这是产前打样——样品只回答一个问题：按这个样子做出来，行不行。

**注意点**

- **出题素材必须直接从向量库抽，不走接入层的检索入口。** 用方向名去检索、拿返回的块出题，等于"用系统能检索到的内容去考系统"——循环论证，指标会虚高。抽出来的块系统可能根本检索不到，那正是要被测出来的东西。
- **每块只出一道题。** 同一块反复出题等于重复样本，会虚高置信度。素材按全量出题数准备，本步只从池子里挑几块打样。
- **几个能代表成品的实例就够了，不需要讲究挑选规则。** 每格一条、最多 20 条：格子数 ≤ 20 时全覆盖，超过时取方向权重最高的 20 个各一条、形态在期间轮换。怎么取自己定，不要为此问用户——样品不完美没有后果，用户不满意再调整就是。
- **每条的答案要点必须露出来。** 用户最容易发现的问题就是"答案抓错了"，只给题目看不出来。
- **样品不是生产清单。** 没打样的格子全量生产时照样出题，无法回答的样本配额一条不少。交样时说明哪些组合没打样，并问用户是否满意、要不要看别的方向或规模；他点名看什么就再打几件。
- **样品可带 `note`，用来指导「怎么写」。** 判据是它改不改生成的内容：改生成什么 → 进规格（改格子配额或写进 `generation.constraints`）；只让写法更像 → 记在 `note` 里。**`note` 是提示不是契约**，跟着范例交给生成者，不参与校验、不进报告。
- **泄漏检查放在这一步。** 拿样品算 query 与源块的 n-gram 重合度，过高说明在照抄原文。在这一步发现比全量生成之后发现便宜得多。报告数字，不自动下结论。
- **迭代过程中不落盘**（样品会反复改，写文件既没用又和「产物不覆盖」打架）；用户点头后写一份 `samples.jsonl` 作为确认稿——它同时是 few-shot 范例、`eval-set` 中 `pilot: true` 条目的来源，以及 `note` 的载体。
- **发现方向或形态本身有问题时，回第④步提升规格版本号**，不在本步就地改。

### ⑥ 全量生成

**目标**：按已冻结的规格生成完整评测集。

**注意点**

- **开跑前必须拿到用户点头。** 这一步会跑几百条，代价远大于打样；**确认过样品不等于同意开全量**，开跑前要说清预计规模与大致耗时。
- **按规格生成，不是按样品生成。** 生成依据是 `spec.size.cells`；**样品只提供风格范例，不决定生成哪些格子**。样品里没有的方向或形态照样要生成——它按权重有配额。这是本步最容易犯的错。
- **格子配额可以与边际权重不同。** `size.cells` 是显式列表，方向与形态之间的关联偏好（例如「问职业时一般用关键词」）只能靠它表达。发现这类偏好要主动问用户——他未必想得到，但被问到时通常立刻能答。此时边际权重只是参考，规格里要写清（规则 15）。
- 分片生成时，每片收到的约束与范例应当完全一致（**含样品的 `note`**）；条目多时用 subagent 或 workflow 并发。
- **校验是确定性的**，适合写成脚本：各格条数与规格逐格一致（尤其没打样的格子）、形态合规、每题有源块、源块不重复、总数对得上。再算一遍泄漏度并与打样时的分布对比，明显变差说明分片跑偏了。
- **要点与源块的一致性筛查**：算最长公共子串比例，把偏低的挑出来。低比例有两个来源——源块本身可读性不高（图片、表格或特殊格式未被正常解析、正文读不通），要点由它整理而来（**已知的预处理缺陷，不改，归到第⑨步「内容对但切分不当」**），或源块可读性正常而要点却几乎不重合（**可疑是编的，要核**）。这是筛查不是判定，名单留给第⑧步。
- **源块质量低是预处理阶段的问题，评测阶段无解**——但它不是「与检索无关」：被压平的块正文本身就是噪声，相关查询会检索不到。它表现为检索失败，根因在上游，所以归因时要单独标出来。
- 无法回答的样本单独配额，不占任何方向或形态的格子，每条注明"为什么库里没有"。
- 用户提供的真实查询样本单独标记来源，其可信度高于生成样本。
- **生成完把选择权交给用户**：先过一遍（交付按方向分组的可读清单），还是直接开始测试。几百条逐条看很累，不要替他决定。
- 审查期间发现规格问题仍然回第④步提版本；只是某几道题写得不好，就地改即可。

### ⑦ 执行与判定

**目标**：对评测集执行检索并完成相关性判定，得到指标。

**注意点**

- **执行检索、判定、聚合三阶段分离，不得边跑边判。** 判定标准在后续步骤中必然需要调整，而检索是最耗时的环节。分离后调整判定标准只需重新判定，无需重新执行检索。
- 原始检索结果一经产生即不再改动，是后续一切结论的证据。判定结果与聚合指标均为派生数据，可重复生成。
- 原始检索结果中必须记录足以还原本次运行的配置信息（所依据的规格版本、所选链路、适配器版本等），否则两次运行的差异无法解释。检索耗时也应一并记录。
- 允许判定与检索流水线并行以提升速度，但原始结果必须先落盘。
- **判定只执行一次，全部指标由同一批判定推导。** 不应为每个指标单独调用模型，否则成本成倍增加，且不同指标基于不同判定，出现矛盾时无法解释。
- 正式运行前应先后判定小批量样本，并向用户展示**判定明细**而非总分：逐条列出查询、检索结果、判定结论及理由。用户能够判断"某条结果被判定为部分相关是否合理"，但无法判断"某个聚合分数是否合理"。
- 小批量样本应分层抽取，并刻意包含最易暴露判定标准缺陷的样本类型，例如超出知识范围的查询、答案分散在多处内容块的查询、以及检索结果中包含高度重叠内容的查询。
- 正式运行前需与用户确认预计耗时、判定调用规模、是否需要额外的凭据、以及失败重试策略。预计耗时应由小批量实测的单条耗时外推得出。
- **判定用当前 DSH 会话配置的模型，插件不自己选、也不管理凭据。** 想换判定模型就改 DSH 的模型与 API 配置（包括本地模型）。**同一模型既生成又判定存在自评偏差**，可提示用户分两步换模型，但不要替他决定。
- **判定明细不要回主 Agent。** 几百条「查询 × 块」的正文加起来会吃掉主上下文；分片交给 subagent 并让它们直接落盘，主 Agent 只收摘要。
- **判定单位是查询而非「查询 × 块」**，一次给出该查询的全部候选块，避免问题与要点重复；判定连同源块一起给出，以便消解要点里的指代。

### ⑧ 效度校验

**目标**：在进入分析之前，确认本次运行结果可信。校验不通过时应指出应当回退到哪一步，而不是笼统地报告评测失败。

**注意点**

- 用户反馈"方向完全不符合预期"属于**回退信号**，不属于校验项。校验针对本次运行的输出，而方向性问题意味着此前某个确认环节出现偏差，应据此定位到相应步骤重新执行。
- 校验来源分为三类：
  - **自动断言**：指标之间的单调性关系、空结果率阈值、各分层样本量与规格的偏差等。此类检查成本最低，应优先执行。
  - **目视复核**：抽取一定比例的判定明细交由用户复核。
  - **模式识别**：部分现象本身即是信号。例如全部方向的得分一致偏低，可能指向判定标准过严而非被测系统效果差；仅个别方向得分偏低，可能指向该方向的题目难度或术语生僻程度，而非检索层的问题。
- **必须检查评测集泄漏。** 若问题由语料内容块生成，且生成时大量沿用了原文措辞，则查询与源内容块的词面高度重合，检索难度被人为降低，指标虚高。应统计查询与其源内容块的词面重合度，识别高重合样本的占比。占比异常时需回退至第⑤步重新打样，并在规格中增加约束。
- **泄漏检查要按形态分开报。** 关键词形态天然使用库里的术语、重合度本来就高；**自然语言形态重合度高才是问题**。笼统给一个总数会掩盖这一点。
- **校验只判断、不修改。** 不通过时输出退回目标，由用户决定；校验过程本身不动任何产物。
- **要点可靠性消费第⑥步的筛查名单。** 当时只筛查不下结论，这里逐条分辨：源块可读性差导致整理（正常，归库的问题）还是要点确实对不上（回第⑥步重出）。
### ⑨ 归因分析

**目标**：由指标得出结论，定位问题层级，给出可执行的改进建议。

**注意点**

- 指标定义可参考现有方法，但以下两项无法沿用：
  - **显著性**：比较不同变体时应使用配对检验。同一批问题渲染为不同形态，样本天然配对，在小样本上比较两个独立均值极易得出错误结论。
  - **失败案例归因**：均值只能指出效果不佳，归因才能指出改进方向。失败案例应按问题层级分类，至少区分：未召回到相关内容、召回后被阈值过滤、召回但排序落在截断范围之外、召回内容正确但切分不当、以及基准答案本身有误。
- **这一步的输入是数据，不是「刚才那次运行」。** 分析方法与数据来源无关——拿人工测评的逐条明细过来同样能接；写 skill 与实现时不要把两者绑死。
- **评判依据是三级阶梯**：有数值验收线就答达标/未达标；有偏好型要求就按偏好判**并据此定建议方向**；两者都没有则只做相对判定。**常识只能判结构性异常**（空结果率异常高、某层接近全败、指标关系不成立），不能判分数高低的绝对水平。
- **报告是一份体检报告**：全部指标都列；已经够好的一行带过并写明「不用动」；需要改的按严重程度与用户偏好展开。**确实没有可改的就直说没有，不要硬凑**——但必须区分「达标」与「没有尺子所以无法判断」。
- **达标就是结论，不是缺陷。** 体检指标有可能完全健康——**尤其用户给过明确指标并且达到了**，那就直接说不用改，不要为了「报告看起来有价值」硬找问题：编出来的优化点会让用户去动本来没问题的环节，把好的改坏。逐层诊断照常逐层给结论，只是全部为「不用动」并各自写明依据。
- **报告纪律：**每个数字带样本量与置信区间；分层结论不超出 `spec.size.reportable`；**均值不够，要报分布**（平均 0.6 可能是「都在 0.6」，也可能是「一半满分一半零分」，改进方向完全不同）。
- **判定不可信时不要报告系统结论。** 目视复核异议率偏高说明判定标准本身有问题，先回第④步改口径重判。
- **库的覆盖度不在归因范围内。** 若评测中撞见「用户问的某类内容库里确实没有」，可以作为顺带发现提一句（建议自然是补语料），但要说明**这不是本次评测的检测项**——覆盖度需要知道库里应该有什么，插件无从判断。
- **建议可以引用项目里已有的环节，但到此为止。** 例如「项目里已有查询改写环节，可以考虑往那个方向调整」。**改代码是评测之后的事**，由用户与 Agent 另行处理，不属于本插件——插件给方向和依据，不负责实施。
- **必须遵守规格里的 `size.reportable`。** 样本量不足的格子只能合并报告或标注为参考，不得单独下结论。
- **归因的核心判据——库的问题还是检索的问题：**
  | 现象 | 归因 |
  |---|---|
  | 要点被命中，且命中的就是最优块，但该块本身可读性不高（图片、表格或特殊格式未被正常解析） | **库/切分的问题** → 建议重新处理语料 |
  | 要点找不到，而对应源块的排版本身正常 | **检索的问题** → 建议改 embedding / chunking / 阈值 |
  | 源块根本不在库里 | 评测集缺陷，而不是系统问题——但这种情形少见，因为题目是**从库里的块反推**出来的 |
  这条判据成立的前提是：判定用的是**整理成可读文本的要点**而不是原文摘录——否则可读性差的源块会把判定淹没，让人把「找到了但块读不通」误判成「没找到」。整理要点不修改源块与向量库，只为让这两种故障可分。
- **基准答案有误这一类必须单独区分。** 否则会把评测集的缺陷计入被测系统，导致对本来无问题的环节进行修改。
- 结论必须能够追溯到聚合指标与判定明细，不得由模型自由判断得出。

---

## 7. 产物与输出目录

### 7.1 输出目录

所有新建产物集中写入一个独立的输出目录，与被测项目的代码和向量库分离。

- **默认位置**为被测工作区下的 `evaluation/<运行标识>/`，路径可配置。被测工作区的判定见第①步。
- 运行标识使用时间戳。**目录结构必须容纳多次运行，不得平铺**——规格变更会提升版本号，不同规格产出的报告不可直接比较。
- **归属标记**：创建 `evaluation/` 时在其中写入标记文件，用于后续识别该目录是否由本插件创建。同名目录也可能属于无关项目，不能仅凭名称假定归属。
- **已有目录的处理**：每次开始工作前先检查该目录是否已存在。
  - 不存在 → 新建，写入归属标记。
  - 存在但无归属标记 → 可能不属于本插件，**不要写入**。向用户说明情况并询问。
  - 存在且有归属标记 → 列出已有运行及其产物，询问用户是**接着某一次继续**（读取该次产物、从下一步接着走），还是**新建一次运行**（与既有运行并存）。
- 重新运行不得覆盖或修改任何既有运行的产物。新建运行不继承既有运行的产物。
- 首次写入前检测输出目录是否位于版本控制仓库内且未被忽略，并向用户提示。

### 7.2 产物分层

评测过程产出以下产物：


| 产物         | 角色                                                     |
| ------------ | -------------------------------------------------------- |
| 系统画像     | 第①②步产出，记录已确认的链路、向量库事实与质量体检结论 |
| 规格         | 第④步冻结，带版本号                                     |
| 评测集       | 第⑤步产出                                               |
| 原始检索结果 | 第⑦步产出，作为证据，产生后不再改动                     |
| 判定结果     | 第⑦步产出，派生数据，可重新生成                         |
| 聚合指标     | 由判定结果推导                                           |
| 校验结论     | 第⑧步产出                                               |
| 报告         | 第⑨步产出                                               |

**分层原则**：分析读取聚合指标与判定明细；当某项结论受到质疑时，向下追溯到原始检索结果。

### 7.3 版本化原则

规格变更提升版本号而非原地覆盖；报告中必须注明所依据的规格版本。

### 7.4 来源标注原则

所有产物中的事实性字段都应标注来源，区分实测、从代码读取、推断、未经验证的假设、用户确认、以及未知。推断值不得与实测值并列呈现而不加区分。

---

## 8. 指标与判定原则

### 8.1 指标

指标定义沿用通行方法。以下为各指标的可用条件与解释上的注意点：


| 指标               | 可用条件               | 注意点                                                       |
| ------------------ | ---------------------- | ------------------------------------------------------------ |
| 命中率             | 无附加条件             | 基准只有一条时，其数值与召回率相同，不应作为两项独立证据呈现 |
| 精确率             | 无附加条件             |                                                              |
| 平均倒数排名       | 无附加条件             | 返回条数很少时区分度低，宜与排序质量类指标配合使用           |
| 归一化折损累计增益 | 需要分级相关性判定     | 若判定只输出二值结果，该指标会退化为平均倒数排名的单调变换   |
| 召回率             | 需要适配器提供文档标识 | 不可计算时应明确标注原因，不以近似值替代                     |

**只报告可计算的指标。** 明确指出某项指标无法计算及其原因，优于提供一个虚假的完整指标体系。

### 8.2 判定

**判定只执行一次，全部指标由同一批判定推导。**

判定标准需要明确的内容包括：判定粒度（基于答案要点还是文档标识）、相关性分级方式、无法回答样本的处理方式、以及被测系统可选模式的处理方式。

其中判定粒度是关键：当切分带重叠窗口时，同一内容会出现在多个相邻块中，基于文档标识的严格匹配会系统性低估指标。

**执行分工**：指标计算全部由插件内的确定性代码完成；相关性判定由插件批量调用模型完成；Agent 负责制定判定标准、抽查判定结果、以及解释结论。逐条明细落盘保存，返回给 Agent 的仅为聚合结果——逐条传递会超出上下文预算。

---

## 9. 已知限制与未决问题

### 9.1 已知限制

- 召回率类指标依赖适配器提供文档标识，不提供时无法计算。
- 合成评测集存在泄漏风险，只能检测与抑制，无法根除。
- 内容块缺少元数据时，失败案例无法溯源到原文位置。
- 样本量有限时，分层指标的置信区间较宽，部分分层的结论不可用。
- 向量库通常不存储嵌入模型信息，模型一致性默认基于查询代码推断。
- **沙箱只约束写，不约束读。** DSH 的三种沙箱模式（`read-only` / `workspace-write` / `danger-full-access`）全部是写边界，文件读取不受限制。因此"证据只能来自被测工作区"是**范围约定**，不是强制边界；真正要隔离只能把 DSH 放进容器并只挂载被测工作区。读越界的代价是时间和 token，不是正确性——所以约束落在"写什么"上：报告只写本步结论。

### 9.2 未决问题

- 模型一致性校验是否应升级为默认执行？
- 是否需要支持跨集合的统一评测？多个集合的标识空间可能重叠。
- 生成式评测的接入时机与前置条件如何确定？
- 与配套构建类插件之间的接口边界如何划定？

---

## 10. 当前实现状态

| 组件 | 状态 |
|---|---|
| 包描述文件 | 已有，含 DSH 插件挂载声明与插件入口声明 |
| 插件挂载配置 | 已有。挂载了 MCP 客户端与 skill 注册两行；前者当前不注册工具，保留备用 |
| 插件入口 | 已有，向 skill 注册表注册本插件自带的 skill |
| 启动脚本 | 已有，含 Python 环境自举与依赖变更检测 |
| 服务入口 | 已可启动，尚未注册任何工具 |
| 声明式执行器 | 已有（`python/executor.py`），含 11 项合成自测（`python/selftest.py`）；已通过 profile 符号链接路径对真实系统验证 |
| 依赖声明 | 已有 |
| 第①步 skill | 已有，分步参考为 `references/step-01-inventory.md` |
| 第②步 skill | 已有，分步参考为 `references/step-02-profile.md` |
| 第③步 skill | 已有，分步参考为 `references/step-03-query-plan.md` |
| 第④步 skill | 已有，分步参考为 `references/step-04-freeze-spec.md` |
| 第⑤步 skill | 已有，分步参考为 `references/step-05-prototype.md` |
| 第⑥步 skill | 已有，分步参考为 `references/step-06-generate-set.md` |
| 第⑦步 skill | 已有，分步参考为 `references/step-07-run-and-judge.md` |
| 第⑧步 skill | 已有，分步参考为 `references/step-08-validate.md` |
| 第⑨步 skill | 已有，分步参考为 `references/step-09-analyze.md` |
| 设计文档 | 本文件 |

### 10.1 skill 的注册方式

skill 不能依靠放置在插件包内的 `skills/` 目录被自动发现。

文件系统 skill 提供方只扫描固定的若干根目录，插件包内的目录不在其中。看起来可以通过 profile 的 patch 层覆盖该提供方的 `customSkillDirs` 来解决，但**在 web profile 下无效**：host 层的 `skill-filesystem` 行被禁用（web 组合包的注释说明"由 preset 负责本地发现"），实际生效的是 agent preset 挂载的、不带配置的另一行，而 profile 的 patch 层作用不到 preset 组合。

因此本插件改为自身作为插件入口，直接向 skill 注册表注册提供方。部署级插件注册进的是全局层，不受上述分层影响。

**约束**：该入口对被注册 skill 的 frontmatter 只支持单行的 `key: value`，不解析多行值。修改 `SKILL.md` 的 frontmatter 时需保持该形式。
